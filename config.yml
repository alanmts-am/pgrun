# Conexão do banco principal onde estão as demais conexões
connection:
  host:
  port:
  database:
  username:
  password:
sql:
  # Query para listar a conexão com os bancos. Deve ser na sequência host, port, database_name, username, password
  database: select db_host, db_port, db_name, db_username, db_password from table
# Opção para indicar se seria uma query do tipo SELECT
options:
  select: False
